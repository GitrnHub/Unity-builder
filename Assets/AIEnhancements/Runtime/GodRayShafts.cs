using UnityEngine;

// Kept as a compatibility placeholder so existing Unity meta references remain valid.
// The previous implementation used translucent geometry around the camera and did not
// produce reliable depth-occluded shafts. SunShaftsRendererFeature now owns this effect.
public sealed class GodRayShafts : MonoBehaviour
{
}
